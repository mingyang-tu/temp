### Blanking (消隱期)
想像你在紙上寫字：
- 當你寫完一行，要把筆提起來，移到下一行的開頭，這段**「提筆位移」**的時間就是 HBlank。
- 當你寫完一整頁，要把筆移回整張紙的最左上角，這段**「翻頁」**的時間就是 VBlank。

### Porch
在顯示訊號的時序圖（Timing Chart）中，一個同步訊號（Sync Pulse）的前後會各有一段空白，這就是 Front Porch（前緣） 和 Back Porch（後緣）。
- Front Porch (前緣)：
  - 發生時機： 有效影像資料（Active Data）結束後，同步訊號發出前。
  - 作用： 告訴顯示器「資料快結束了，準備收尾」。
- Sync Pulse (同步脈衝)：
  - 作用： 這是最重要的節奏點。它告訴 TCON：「現在就是第 $N$ 行的結束」或是「現在就是第 $M$ 幀的結束」。
- Back Porch (後緣)：
  - 發生時機： 同步訊號結束後，下一段有效影像資料開始前。
  - 作用： 最忙的一段時間。顯示器會利用這段時間來穩定電路（例如穩定電壓、對齊時鐘），確保下一行像素進來時，顏色是準確的。

### Pulsed Backlight
液晶顯示器（LCD）有一個天生的缺點叫做 Sample-and-Hold（採樣保持）。
- 問題： 即使你的 Overdrive（OD）做得再好，讓像素在 1ms 內就轉到位，但因為背光在整個影格週期（例如 144Hz 的 6.9ms）內是一直亮著的，當你的眼睛跟著畫面中的物體移動時，大腦會把不同時間點的影像疊加在一起，產生「動態模糊」。
- 解法： 在像素轉動完成後，背光才「閃」一下（Pulse）。在像素準備切換到下一張圖時，背光立刻關閉。
