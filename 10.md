# 色彩學 Ch10

## RGB 色空間的定義方式
RGB 色空間標準化的三個主要參數為：

1. Gamma 值 (TRC 轉換)
2. 白色 (white point) 的色度座標
3. RGB 三原色 (RGB primaries) 的色度座標

## 重要的 RGB 色空間標準

### TV 的 RGB 標準
- NTSC：美國國家電視系統委員會 (NTSC) 於 1953 年制定的彩色電視廣播標準。
- PAL/SECAM：歐洲廣播聯盟 (EBU) 於 1966 年制定的標準，後續被歐洲的 PAL 播放系統與法 / 俄的 SECAM 系統所採用。
- SMPTE-C (SDTV)：電影與電視工程師協會 (SMPTE) 於 145 號報告「SMPTE-C: Color Monitor Colorimetry」中，為標準畫質電視系統 (SDTV) 定義的標準。
- HDTV：於 1990 年制定的高畫質電視系統標準。

### 顯示器的 RGB 標準
- sRGB：1996 年，HP 與 Microsoft 公司意識到缺乏世界統一的顯示器色彩標準將對數位攝影、網路購物等應用造成極大的困擾，因此建議所有廠商採用一個標準的 RGB 空間，稱為 sRGB (standard RGB)。

### 廣色域的 RGB 標準
上述標準都是基於映像管 (CRT) 的色彩特性制定的。然而，在數位相機與印刷媒體上有更多鮮豔的可複製色彩，是 sRGB 無法記載的。因此需要制定色域更廣的 RGB 空間來精確複製這些標準螢幕上看不到的鮮豔色彩。
- CIERGB
- Wide Gamut RGB
- Adobe RGB：Adobe 於 1998 年提出的標準，讓數位相機與印刷媒體大部分的色彩能夠用 8-bit 的 RGB 色階，在不產生明顯跳階問題的環境下記錄下來。因此特別受到攝影師、設計師及出版業者的愛用。

## 視訊色彩標準
以 NTSC 標準傳送視訊時，必須先將 RGB 轉成亮度與色差訊號 (如 YIQ)。目的是將更重要的亮度訊號 (如 Y) 使用較大的頻寬傳送；而視覺上不敏感的色差訊號 (如 IQ)，透過取樣與量化，使用較窄的頻寬傳送。
與 YIQ 類似的還有歐洲 PAL/SECAM 系統的 YUV、ITU 國際通信組織的 YCbCr 等。

    ┌ Y ┐   ┌ 0.299,  0.587,  0.114 ┐┌ R ┐
    │ I │ = │ 0.596, -0.275, -0.322 ││ G │
    └ Q ┘   └ 0.211, -0.523,  0.312 ┘└ B ┘

    ┌ Y ┐   ┌ 0.299,  0.587,  0.114 ┐┌ R ┐
    │ U │ = │-0.147, -0.289,  0.436 ││ G │
    └ V ┘   └ 0.615, -0.515, -0.100 ┘└ B ┘

    ┌  Y ┐       ┌ 0.299,  0.587,  0.114 ┐┌ R ┐   ┌  0  ┐
    │ Cb │     = │-0.169, -0.331,  0.500 ││ G │ + │ 128 │
    └ Cr ┘jpeg   └ 0.500, -0.419, -0.081 ┘└ B ┘   └ 128 ┘

    ┌  Y ┐       ┌ 0.257,  0.504,  0.098 ┐┌ R ┐   ┌  16 ┐
    │ Cb │     = │-0.148, -0.291,  0.439 ││ G │ + │ 128 │
    └ Cr ┘mpeg   └ 0.439, -0.368, -0.071 ┘└ B ┘   └ 128 ┘

這裡的 Y 指的不是 CIEXYZ 裡定義的亮度 Y (Luminance)，而是未經 gamma 校正的非線性亮度值，簡稱 Luma。

## 與 sRGB 相容的廣色域色彩標準
由於前面提到的 CIERGB、Wide Gamut RGB、Adobe RGB 無法跟 sRGB 相容，業者又設計的一些與 sRGB 相容的廣色域 RGB 標準。
他們沿用了 sRGB 標準所採用的 HDTV 座標，但是容許 gamma 校正後的 RGB 值超出 0% ~ 100% 的範圍。
這些標準通常使用超過 8-bit 的量化解析度來描述 RGB 色階的變化。

- e-sRGB：HP 提出的廣色域色彩標準，有 10-bit、12-bit、16-bit 的版本。各原色能記錄的亮度範圍是 sRGB 的 -53% ~ +168%。
- bg-sRGB：10-bit 的版本能記錄的亮度範圍是 sRGB 的 -50% ~ +150%，此標準並沒有被廣泛使用。
- scRGB：微軟提出的廣色域色彩標準，有 12-bit 的非線性 RGB 格式與 16-bit 的線性 RGB 格式。各原色能記錄的亮度範圍是 sRGB 的 -50% ~ +750%。

- sYCC：根據 sRGB 定義的 YCbCr 空間。
- scYCC：根據 scRGB 定義的 YCbCr 空間 (12-bit)。
- xvYCC：支援 8-bit，而且色域比 sRGB 更大 (仍略小於 Adobe RGB)

在 xvYCC 中，100% 擴散反射標準白的 YCbCr 值是 (235, 0, 0) 而不是 (255, 0, 0)，這是為了讓 xvYCC 記錄影像中高亮度的鏡面反射。這些高亮度的色度值會被壓縮存放在 Y = 235 ~ 255 之間。
