# 色彩學 Ch7

## 顯示器色光混色原理
- LCD 是採用色光加法中併置混色的方式產生顏色。
- 格拉斯曼法則 (Grassman Law)

## 「黑色浮底」現象
當色彩輸入訊號是黑色時 ((R, G, B) = (0, 0, 0))，本應沒有任何刺激量產生，但卻溢出少許刺激量的現象。
- 通常是由面板內部少許訊號互相干擾或是液晶分子漏光所造成的結果。
- Cross Talk

## 階調 (Tone)
平面顯示器的階調是指畫面再現紅、綠、藍色階時，不同色階與亮度的關係。一般是使用「γ 曲線」、「階調複製曲線」(TRC, Tone Reproduction Curve) 或「光電轉換函數」(OETF, Optoelectronic Transfer Function) 等來描述。
簡單來說，階調就是顏色訊號與發光亮度的關係。「γ 曲線」、「TRC」和「OETF」對於 LCD 面板的意義一樣，就是為了將訊號轉成實際的亮度值。

## 色域 (Color Gamut)
色域指的是影像輸入輸出設備所能表現色彩的最大範圍。色域面積越大，代表能容納的顏色越多。

## 顯示系統色彩變換流程

input：8-bit RGB

1. 正規化 (Normalization)：
    R_0 = R / 255
    G_0 = G / 255
    B_0 = B / 255

2. TRC 轉換 (TRC Transformation)：描述顯示系統中輸入訊號與發光亮度的關係，此處以 γ 曲線舉例。
    R_γ = R_0^(γ_R)
    G_γ = G_0^(γ_G)
    B_γ = B_0^(γ_B)

3. 線性轉換 (Linear Transformation)：利用一個 3x3 矩陣，在線性 RGB 與三刺激值 XYZ 之間進行線性變換。
    ┌ X ┐     ┌ R_γ ┐   ┌ X_k ┐   ┌ X_R X_G X_B ┐┌ R_γ ┐   ┌ X_k ┐
    │ Y │ = M │ G_γ │ + │ Y_k │ = │ Y_R Y_G Y_B ││ G_γ │ + │ Y_k │
    └ Z ┘     └ B_γ ┘   └ Z_k ┘   └ Z_R Z_G Z_B ┘└ B_γ ┘   └ Z_k ┘
    
    [X_k Y_k Z_k] 代表黑色色塊 ((R,G,B) = (0,0,0))的 XYZ 值。

4. 色度轉換 (Chromaticity Transformation)：
    從 [X Y Z] 轉換成 [x y Y] 或是 [L* a* b*]

5. 色知覺轉換 (Color Perception Transformation)：
    從 [L* a* b*] 轉成 [L C h]

## 顯示設備色域視覺化
