# 色彩學 Ch4

### Gamma 校正

> - 由於顯示器的亮度與所加的電壓成非線性關係，其關係為 I ~ (V_s)^γ'。
> - 對 CRT (陰極射線管) 而言，γ' 大約為 2.2 ~ 2.5。此數值對不同規格或電訊標準而有些微差異。
> - 一般而言，彩色顯示器的 RGB 三色分別有其 γ' 值，但在簡化的系統設計中通常採用單一 γ' 值應用於三色。

Gamma 校正的方法為將電壓訊號做一相反的轉換，使得輸出亮度與訊號呈線性關係。

輸入訊號 -> Gamma 校正 -> I ~ (V_s)^γ' -> 修正後線性

由於原始的 Gamma 校正函數在靠近 0 處斜率會接近無限大。因此轉換公式靠近原點的地方有一段改用線性函數，如下圖。

![](image/transfer709.gif)

- 實作上通常以韌體程式處理或用 LUT 直接替代數值。
- 考慮人眼視覺系統的影響，圖中函數的 exponent γ (1/γ') 通常設定在 0.33 ~ 0.45 之間。

### LCD 影像顯示器

![](image/LCD_structure.jpg)

- 彩色濾光陣列：使用不同的 RGB 排列方式讓視覺上達到更好的混合效果。例如：RGB Delta 型。
- 顯示器色域係數 (color gamut %)：
    - 指螢幕能顯示的色彩範圍。
    - 計算方式為將顯示器的 RGB 原色的色度座標測出，如 (a1, b1), (a2, b2), (a3, b3)，再用公式計算出三角形面積，其面積之百分比即為色域係數。
    - LCD 的色域係數通常比 CRT 來的小。
- 藍偏移 (blue shift)

